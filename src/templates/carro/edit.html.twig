{% extends 'base.html.twig' %}

{% block title %}Edit Carro{% endblock %}

{% block body %}
    <h1>Edit Carro</h1>

    {{ include('carro/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('carro_index') }}">back to list</a>

    {{ include('carro/_delete_form.html.twig') }}
{% endblock %}
