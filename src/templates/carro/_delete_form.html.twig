<form method="post" action="{{ path('carro_delete', {'id': carro.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carro.id) }}">
    <button class="btn">Delete</button>
</form>
